Hamming Distance
Time complexity : O(logn)
Space complexity : O(1)

int hammingDistance(int x,int y){
    int count=0;
    while(x>0 || y>0){
        int rem1=x%2;
        int rem2=y%2;
        if(rem1!=rem2)
        count++;
        x=x/2;
        y=y/2;
    }
    return count;
}
