1) Largest in Array
Time complexity : O(n)
Space compelxity : O(1)

int largest(vector<int>& arr){
     int n=arr.size();
     int maxi=INT_MIN;

     for(int i=0;i<n;i++){
         if(maxi<arr[i])
         maxi=arr[i];
     }
     return maxi;
}

